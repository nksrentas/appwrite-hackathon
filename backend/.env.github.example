# GitHub Integration Environment Variables
# Copy these to your .env file and configure with your GitHub App credentials

# GitHub OAuth Configuration
# Create a GitHub App at: https://github.com/settings/apps
GITHUB_CLIENT_ID=your_github_app_client_id
GITHUB_CLIENT_SECRET=your_github_app_client_secret
GITHUB_OAUTH_REDIRECT_URI=http://localhost:3000/auth/github/callback

# GitHub Webhook Configuration
# Base URL for webhook endpoints (should match your deployed backend URL)
WEBHOOK_BASE_URL=http://localhost:3001

# GitHub Security Configuration
# Generate a strong 32+ character encryption key for token storage
GITHUB_ENCRYPTION_KEY=your_very_long_encryption_key_at_least_32_chars

# GitHub App Permissions Required:
# - Repository permissions:
#   - Contents: Read (to read repository content)
#   - Metadata: Read (to read repository metadata)
#   - Pull requests: Read (to track PR activities)
#   - Actions: Read (to track workflow runs)
# 
# - Account permissions:
#   - Email addresses: Read (to get user email)
#
# - Subscribe to events:
#   - Push
#   - Pull request
#   - Workflow run
#   - Deployment
#   - Deployment status

# Optional: GitHub Enterprise Configuration
# GITHUB_API_BASE_URL=https://api.github.com
# GITHUB_OAUTH_BASE_URL=https://github.com

# Optional: Rate Limiting Configuration
# GITHUB_RATE_LIMIT_REQUESTS_PER_HOUR=5000
# GITHUB_RATE_LIMIT_SECONDARY_REQUESTS_PER_HOUR=60

# Optional: Webhook Security
# GITHUB_WEBHOOK_SECRET=your_webhook_secret_from_github_app

# Optional: Monitoring Configuration
# GITHUB_MONITORING_INTERVAL_MINUTES=5
# GITHUB_HEALTH_CHECK_TIMEOUT_MS=30000
# GITHUB_METRICS_CACHE_TTL_MINUTES=5

# Optional: Maintenance Configuration
# GITHUB_OAUTH_STATE_RETENTION_HOURS=24
# GITHUB_ACTIVITY_EVENT_RETENTION_DAYS=30
# GITHUB_WEBHOOK_FAILURE_THRESHOLD=5

# Optional: Development/Testing Configuration
# GITHUB_DISABLE_WEBHOOK_SIGNATURE_VERIFICATION=false
# GITHUB_ENABLE_DEBUG_LOGGING=false
# GITHUB_MOCK_API_RESPONSES=false